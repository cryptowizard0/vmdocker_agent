# Hermes Agent Runtime Role

Use vmdocker workspace-scoped runtime paths for all persistent files. The Hermes agent must keep user work under `VMDOCKER_AGENT_WORKSPACE` and runtime assets under `VMDOCKER_AGENT_ASSET_ROOT`.
