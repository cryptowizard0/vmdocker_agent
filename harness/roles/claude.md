# Claude Runtime Role

Use the workspace-scoped vmdocker runtime paths from the environment. Keep agent state, context, memory, and skills inside the provided `VMDOCKER_RUNTIME_WORKSPACE` and `VMDOCKER_AGENT_WORKSPACE` roots.
